# Peer ranges on three-edge-projection@0.0.10 are stale (^0.155.0 / ^0.6.0)
# while its own lockfile was tested with three@0.184.0 and three-mesh-bvh@0.9.9.
# Keep this so npm ci / npm install stay reproducible on the locked set.
legacy-peer-deps=true
