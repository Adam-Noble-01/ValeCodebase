# ValePlanner Version Locked Dependencies

This directory stores runtime third-party libraries pinned to explicit versions for deterministic local-host behavior.

## Current Runtime Dependencies

- `chart.umd.4.4.3.min.js` (Chart.js v4.4.3)

No other runtime dependencies are currently required by ValePlanner.
