## Optional ONNX Runtime Wheel Cache

Place pre-downloaded Python wheels here when offline installation support is required.

Example:
- `onnxruntime-<version>-cp<py>-cp<py>-win_amd64.whl`

This folder is optional and can stay empty.
