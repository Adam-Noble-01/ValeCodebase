# -----------------------------------------------------------------------------
# VALEVISION3D - EMAIL WORKER - SECRETS TEMPLATE
# -----------------------------------------------------------------------------
#
# Copy this file to `.dev.vars` and fill in the real values.
# `.dev.vars` is loaded automatically by `wrangler dev` and is gitignored.
#
# For production, set these via:
#   wrangler secret put MICROSOFT_CLIENT_SECRET
#   wrangler secret put EMAIL_ADDRESSBOOK_KEY_B64
#   wrangler secret put R2_SECRET_ACCESS_KEY
#
# All non-secret config (tenant IDs, sender user, rate limits, allowed
# origins, etc.) lives in wrangler.jsonc under "vars".
#
# -----------------------------------------------------------------------------

MICROSOFT_CLIENT_SECRET={{REDACTED}}
EMAIL_ADDRESSBOOK_KEY_B64={{REDACTED}}
R2_SECRET_ACCESS_KEY={{REDACTED}}
